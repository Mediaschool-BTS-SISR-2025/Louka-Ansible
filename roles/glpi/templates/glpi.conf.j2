<VirtualHost *:80>
    DocumentRoot {{ glpi.install_dir }}

    <Directory {{ glpi.install_dir }}>
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/glpi_error.log
    CustomLog ${APACHE_LOG_DIR}/glpi_access.log combined
</VirtualHost>
